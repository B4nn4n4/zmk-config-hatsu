# User options applied on top of the board defconfigs.

# --- Temporary battery diagnostic ---
# USB CDC logging needs the CDC console, which conflicts with the Studio RPC
# snippet. Studio is disabled and the snippet dropped from build.yaml for now.
# Restore both after diagnosing the ~0% battery report.
CONFIG_ZMK_USB_LOGGING=y

# ZMK Studio (also requires the studio-rpc-usb-uart snippet in build.yaml)
# CONFIG_ZMK_STUDIO=y
# CONFIG_ZMK_STUDIO_LOCKING=n

# Mouse FTW
CONFIG_ZMK_POINTING=y
