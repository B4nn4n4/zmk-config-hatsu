# User options applied on top of the board defconfigs.
#
# ZMK Studio: needs a zmk,physical-layout (keys prop) and removal of
# zmk,matrix-transform chosen first. Then re-enable with the
# studio-rpc-usb-uart snippet in build.yaml (left half only).
# CONFIG_ZMK_STUDIO=y
