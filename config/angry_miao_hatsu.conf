# Shared Kconfig defaults for both Hatsu halves.
# Board-specific options belong in boards/arm/angry_miao_hatsu/*_defconfig.

# ZMK Studio (requires USB on the left/central half)
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
